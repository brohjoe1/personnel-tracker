apiVersion: apps/v1
kind: Deployment
metadata:
  name: personnel-tracker
  namespace: brohjoe1-dev
  labels:
    app: personnel-tracker
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: personnel-tracker
  template:
    metadata:
      labels:
        app: personnel-tracker
    spec:
      containers:
      - name: personnel-tracker
        image: ghcr.io/brohjoe1/personnel-tracker:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"
        - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
          value: "kafka:9092"
        - name: SPRING_KAFKA_ADMIN_FAIL_FAST
          value: "false"
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
          requests:
            memory: "256Mi"
            cpu: "250m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 45
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 30
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
